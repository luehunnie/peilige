# Host port for the Peilige static site (the container also listens on
# 8080 internally — unprivileged, see deploy/Dockerfile).
# Copy this file to deploy/.env to override the default.
PEILIGE_PORT=8080
